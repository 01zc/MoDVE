% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_suitability_matrix.R
\name{create_suitability_matrix}
\alias{create_suitability_matrix}
\title{Create suitability matrix}
\usage{
create_suitability_matrix(
  Microhabitat,
  SpeciesPool,
  use_parabolic_light = TRUE,
  path_to_output = NULL
)
}
\arguments{
\item{Microhabitat}{a matrix containing the surface area, light and
optionally microclimatic conditions in each voxel, as created by
\code{\link[=create_microhabitat_mat]{create_microhabitat_mat()}}}

\item{SpeciesPool}{a data frame containing the species traits, as created by
\code{\link[=draw_species_traits]{draw_species_traits()}}.}

\item{use_parabolic_light}{boolean, if \code{TRUE} (default), the light suitability score is
calculated using the parabolic response curve instead of the cardinal score.}

\item{path_to_output}{name and path of a \code{.rds} or \code{.h5} file to save the suitability
matrix in. If \code{NULL} the matrix is returned instead.}
}
\value{
if \code{path_to_output == NULL}, returns a 4 dimension array with dimensions
x, y, z, number of species, where each layer contains the aggregated suitability scores
for each species.
}
\description{
Pre-compute global environmental suitability scores between 0 and 1 for each species.
}
\details{
Cardinal suitability scores (see \code{\link[=calc_suitability]{calc_suitability()}}) are first calculated
for each environmental (light, temperature, humidity, wind) layer present in
\code{Microhabitat}. Global suitability scores are then computed as the product of
these scores for each species.
}
